#include <stdio.h>

/**
 * main - prints the lowercase alphabets in reverse.
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;
	{
		putchar(ch);
	}
	putchar('\n')
